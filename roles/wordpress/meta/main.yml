---
galaxy_info:
  author: nanaha1003
  description: >
    Deploys WordPress (PHP-FPM) with a MariaDB backend.
    Caddy serves the blog via php_fastcgi; the WordPress HTML volume is
    also mounted by the proxy role for static file serving.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Fedora
      versions:
        - "43"

dependencies:
  - role: proxy
